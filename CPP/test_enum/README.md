## 测试枚举类的继承能力

* 以下代码是在cocos里看到的

```c++
enum class ValueType: char
    {
        INVALID,
        VOID,
        INTEGER,
        LONG,
        FLOAT,
        BOOLEAN,
        STRING,
        VECTOR,
        FUNCTION
    };
 ```
