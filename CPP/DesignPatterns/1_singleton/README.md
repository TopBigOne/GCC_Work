# C++单例模式学习

## 什么是单例模式？
单例模式是一种创建型设计模式，它确保一个类只有一个实例，并提供全局访问点。

## 核心要素
1. **私有构造函数**：防止外部创建实例
2. **静态实例指针/引用**：保存唯一实例
3. **公有静态访问方法**：提供全局访问点
4. **禁用拷贝构造和赋值**：确保单例性

## 应用场景
- 日志记录器
- 数据库连接池
- 配置文件管理器
- 线程池
- 缓存管理

## 学习计划

### 1. 懒汉式单例（Lazy Singleton）
- 特点：需要时才创建实例
- 优点：延迟初始化，节省内存
- 缺点：需要考虑线程安全

### 2. 饿汉式单例（Eager Singleton）
- 特点：程序启动时就创建实例
- 优点：天然线程安全，简单
- 缺点：可能浪费内存

### 3. 线程安全单例
- 使用互斥锁保证线程安全
- 双重检查锁定模式

### 4. Meyers单例（推荐）
- C++11后的最佳实践
- 利用局部静态变量的特性
- 线程安全且高效

## 代码结构
```
singleton/
├── lazy_singleton.h        # 懒汉式单例
├── eager_singleton.h       # 饿汉式单例
├── thread_safe_singleton.h # 线程安全单例
├── meyers_singleton.h      # Meyers单例
├── main.cpp               # 测试代码
└── CMakeLists.txt         # 构建配置
```